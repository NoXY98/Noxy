import React from 'react';

// The API key is now handled exclusively via process.env.API_KEY as per coding guidelines.
// This component is no longer used but kept as a placeholder if imports exist.
const ApiKeyInput: React.FC<any> = () => {
  return null;
};

export default ApiKeyInput;
